{
  "name": "chat",
  "version": "1.0.0",
  "description": "1) Make sure you have mongo installed. Run mongod from terminal. Run mongo from terminal \r 2) Create a db called chat\r 3) Inside the root folder open terminal. 'node server.js' hit Enter\r 4) Open browser. localhost:3000",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "gilbert k",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.0",
    "mongodb": "^2.2.14",
    "socket.io": "^1.5.1"
  },
  "devDependencies": {}
}
