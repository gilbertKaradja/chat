1) Make sure you have mongo installed. Run mongod from terminal. Run mongo from terminal 
2) Create a db called chat
3) Inside the root folder open terminal. 'node server.js' hit Enter
4) Open browser. localhost:3000

Chat will open. Open two tabs for 2 users. Test.